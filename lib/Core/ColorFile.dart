import 'package:flutter/material.dart';

class AppColors {
  // Primary color
  static const Color primaryColor = Color(0xFFCC332B);

  // Background and surface colors
  static const Color backgroundColor = Colors.white;
  static const Color surfaceColor = Colors.white;
  static const Color defaultblack = Color(0xFF373C3B);

  // Text colors
  static const Color primaryTextColor = AppColors.defaultblack;
  static const Color secondaryTextColor = Color(0xFF757575); // Grey text

  // Button colors
  static const Color buttonColor = Color(0xFFCC332B); // Primary button
  static const Color buttonTextColor = Colors.white;

  // Border and divider colors
  static const Color borderColor = Color(0xFFE0E0E0); // Light grey border
  static const Color dividerColor = Color(0xFFE0E0E0); // Divider color

  // Icon colors
  static const Color iconColor = AppColors.defaultblack;

  // Accent colors
  static const Color accentColor = Color(0xFF009688); // Teal for accents
}
